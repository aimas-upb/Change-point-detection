class StatisticsData:
    def __init__(self, precision, recall, accuracy, threshold, match_interval):
        self.precision = precision
        self.recall = recall
        self.accuracy = accuracy
        self.threshold = threshold
        self.match_interval = match_interval
